---
title: "Детерминистические кошельки, BIP32, гомоморфизм"
date: 2020-05-06T00:00:00+00:00
tags: [Биткоин, Криптография]
---

Как известно, Биткоин-адреса должны использоваться только один раз. Само название «адрес» тут даже не совсем уместно — это, скорее, одноразовые инвойсы (как в Лайтнинге 😉). С давних времен правильные Биткоин-кошельки создавали пачку адресов, каждый из которых использовался только один раз. До появления BIP32 кошельки генерировали сотню приватных ключей — и это было очень неудобно: каждый нужно было хранить, защищать и бэкапить. О переносе  ключей между кошельками тогда не было и речи. Для решения этих проблем в далеком 2012 году был представлен BIP32, описывающий детерминистическую и иерархическую схему создания приватных и публичных ключей в виде дерева, в корне которого — мастер-ключ. Благородя такому решению, кошелькам теперь достаточно хранить один производный ключ, из которого можно создавать адреса. Разберем всю схему подробно.

В корне иерархического дерева лежит **сид** (seed) — случайная последовательность байт длиной от 16 до 64 байт (обычно 32). Эта последовательность задает всё дерево и все адреса, крайне важно, чтобы она хранилась надежно. Та самая сид-фраза, которую создают кошельки, это просто способ представления сида в читаемом виде, в виде последовательности слов. Доступ к сиду или сид-фразе дает доступ ко всем производным приватным и публичным адресам.

[В посте про криптографию Биткоина](https://blockndev.com/posts/cryptography-in-bitcoin/) я рассказывал о том, что приватный ключ это просто произвольное число: мы можем генерировать его случайно, можем сами выбрать какое-то конкретное число или последовательность байт, а можем производить его с помощью каких-либо алгоритмов, в том числе криптографических — Биткоин не обязывает нас использовать какой-то конкретный способ. BIP32 использует эту свободу для генерации мастер-ключа и всех производных ключей.

**Мастер-ключ** создается напрямую из сида по формуле:
```
HMAC-SHA512(Key = "Bitcoin seed", Data = Seed)
```
`HMAC-SHA512` эта улучшенная хэш-функция, которая дополнительно защищает данные секретным ключом. В этом конкретном  случае секретным ключом является __Bitcoin seed__, а данными — сам сид.   Функция возвращает 64 байта, первая половина которых (32 байта) используется для создания производных ключей, а вторая половина — это код цепи. Код цепи это некоторый псевдослучайный идентификатор уровня дерева, который используется в качестве секретного ключа (Key) для генерации дочерних ключей.
Совокупность обеих частей называется **расширенным (приватным или публичным) ключом** (при кодировании таких ключей в Base58 используется префикс `xprv` или `xpub`). Каждый узел дерева это расширенный приватный (или публичный) ключ.

Разработчики могут использовать эту функцию для построения любых иерархий, но стандарт предлагает следовать такой схеме:
1. В корне дерева мастер-ключ.
2. От мастер-ключа образуются ключи аккаунтов.
3. У каждого аккаунта два узла: один для генерации внешних адресов, другой — для внутренних адресов. Внешние адреса передаются другим людям для получения от них платежей, а внутренние адреса используется для возврата сдачи.
4. Адреса являются листьями дерева и не создают дочерних ключей.

Но это не требование, а просто рекомендация. BIP44 развивает и улучшает эту схему, добавляя поддержу разных монет — такую схему можно использовать для хранения на одной сид-фразе не только биткоинов, но и любых других монет.

Общая схема, надеюсь, понятна. Теперь углубимся в детали и разберемся, как создаются производные приватные и публичные ключи.

BIP32 описывает несколько формул для создания дочерних ключей:
1. Приватный ключ → приватный ключ.
2. Публичный ключ → публичный ключ. Это самый интересный кейс в BIP32: на продакшн-сервере интернет-магазина можно хранить один публичный ключ, от которого будут генерироваться публичные ключи для приема платежей. Если сервер взломают, то биткоины будут в безопасности, т. к. доступа к приватным ключам у взломщиков не будет.
3. Приватный ключ → (дочерний) публичный ключ. Не стоит путать эту формулу с обычным получением публичного ключа — тут создается дочерний публичный ключ.

Во всех из них для создания ключей используется одна и та же функция — HMAC-SHA512 — которая возвращает 64 байта, первая половина из которых используется для создания ключа, а вторя половина используется как код цепи. HMAC-SHA512 работает так же, как и хэш функции (конкретно эта функция использует SHA-512), но дополнительно требует секретный ключ, что усложняет взлом хэша (для подбора нужно знать секретный ключ). В качестве секретного ключа всегда выступает код цепи родительского ключа, о чем я уже писал выше. Так же, как и функции хэширования, HMAC принимает данные (будем называть их сообщением), хэш которых она и возвращает.

В каждой формуле используется три параметра:
1. Ключ-родитель. Это может быть приватный (`kpar`, строчная буква `k`) или публичный (`Kpar`, прописная буква `K`).
2. Код цепи `cpar`, уникален для каждого уровня дерева.
3. Индекс `i`, порядковый номер узла на уровне дерева. Индексы делятся на две группы: от 0 до 231-1 — обычные, от 231 до 232-1 — усиленные. Зачем такое разделение, увидем дальше.

Разберем каждый способ по-отдельности:
1. **Приватный → приватный.** Для обычных индексов используется сообщение `point(kₚₐᵣ) || i` (`||` — конкатенация): от приватного ключа образуется публичный (публичный ключ это точка на эллиптической кривой, смотри прошлый пост про криптографию), который в сериализованном виде (33 байта) просто объединяется с индексом. Сообщение хэшируется функцией HMAC-SHA512, и **первая половина хэша складывается с родительским приватным ключом** — получается новый дочерний приватный ключ. Суть: мы хэшируем какие-то данные, складываем хэш с родительским приватными ключом и получаем новый ключ — мы можем так делать, потому что Биткоин не предъявляет никаких требований к алгоритму создания приватного ключа. Приватный ключ + случайные данные = новый приватный ключ. Для усиленных индексов используется сообщение: `0x00 || kpar || i`, т. е. уже приватный ключ участвует напрямую — это ключевое различие, которое делает усиленные индексы более надежными. `0x00` здесь добавляется, чтобы длина приватного ключа была равна 33 байт, как у публичного. **Обычные индексы дают больше возможностей, но они менее безопасны.** Почему, обсудим ниже.
2. **Публичный → публичный.** Этот способ работает только на обычных индексах. Сообщение такое: `Kpar || i`. Да, то же самое, что и в предыдущем пункте для обычных индексов. Но разница в том, что первая половина 64-байтового хэша используется как **приватный ключ, от которого берётся публичный, который складывается с родительским публичным** — опять магия: берём хэш, считаем его приватным ключом, находим его публичный и складываем с родительским публичным. **А что, так можно было?** Вопрос, над которым тут стоит подумать: где взять приватный ключ от этого дочернего публичного ключа? Ответ ниже.
3. **Приватный → публичный.** С одной стороны, тут все просто и понятно: от родительского приватного образуем дочерний приватный и просто берем его публичный. И такой способ будет работать для обоих типов индексов. Но, для обычных индексов возможно образовать публичный ключ от публичного родительского, без генерации дочернего приватного ключа. Это возможно благодаря гомоморфизму.

**Что такое гомоморфизм?**
Википедия даёт такое определение:

> Гомоморфизм — это морфизм в категории алгебраических систем. Это отображение алгебраической системы А, сохраняющее основные операции и основные отношения.

Но для нас оно слишком сложное. Для удовлетворения нашего любопытства достаточно такого описания:
> Операции над публичными ключами можно переносить на соответствующие им приватные ключи. И наоборот.

Если мы складываем два публичных ключа `A` и `B`, то получаем новый публичный ключ `C`. Если на него перевести биткоины, то как потом их вывести? Ведь нужен приватный ключ.
Ответ прост: нужно сложить два приватных ключа `a` и `b`, получим приватный ключ `c`, соответствующий публичному `C`.

**Гомоморфизм — важное свойство, которое используется ещё в нескольких других техниках Биткоина. О них я расскажу в следующих постах.**

Вернёмся к BIP32.

Разделение на обычные и усиленные индексы было сделано для того, чтобы разделить образование ключей с помощью гомоморфизма и без него. Обычные индексы используют гомоморфизм, усиленные — нет. Гомоморфизм даёт больше возможностей, но и создаёт больше рисков для безопасности (в этом конкретном случае).

Рассмотрим ещё раз образование ключей с обычными индексами:
1. **Приватный → приватный.** Хэшируется родительский публичный ключ, хэш складывается с родительским приватным, получается дочерний приватный.
2. **Публичный → публичный.** Опять хэшируется родительский публичный, хэш считается приватным ключом, от него берётся публичный, который складывается с родительским публичным, получается производный публичный. Гомоморфизм тут в том, что мы складываем публичный ключ (от хэша) с публичным ключом (родительским). Чтобы узнать производный приватный ключ, нам надо сложить хэш (чтобы его получить, надо знать код цепи родителя и индекс) и приватный родительский ключ.

Что происходит, если взломщику становится известен родительский публичный ключ (расширенный)? Он узнаёт код родительской цепи и публичный ключ. Индексы берутся не из такого уж и большого диапазона, поэтому их можно подобрать. Итого: взломщик может узнать 64-байтовый хэш, который участвовал в создании производного ключа. Что происходит, если взломщик к тому же узнал производный **не усиленный** приватный ключ? Он получает возможность узнать **родительский приватный ключ**: для этого нужно будет вычесть и производного первую половину хэша. Получив родительский приватный ключ, взломщик может узнать все производные от него ключи, как усиленные так и обычные.

Такой сценарий возможен только для обычных ключей, потому что в генерации дочернего приватного ключа они используют хэш публичного. Усиленные ключи хэшируют родительский приватный ключ. А если взломщик получит доступ к усиленному публичному ключу, то он не сможет использовать его в хэширующей функции.

Ну и последний вопрос: почему нельзя использовать усиленные ключи в формуле __Публичный ключ → публичный ключ__? Ответ опять же в гомоморфизме. Вспомним наше описание: 

> Операции над публичными ключами можно переносить на соответствующие им приватные ключи.

Операция должна быть та же самая. Если мы что-то (в данном случае публичный ключ) добавляем к публичному ключу, то то же самое мы должны добавить к приватному, если хотим получить дочерний приватный ключ. С обычными индексами это возможно, потому что в формулах __Приватный → приватный__ и __Публичный → публичный__ кэшируется родительский публичный ключ. Но при использовании усиленных индексов в формуле __Приватный → Приватный__ кэшируется приватный ключ, который в формуле __Публичный → Публичный__ недоступен.

Если всё упросить и свести к какому-то одной мысли, то можно представить, что во всей этой схеме есть только одна функция — HMAC-SHA512 — которая возвращает хэш существующего ключа, и этот хэш используется для создания производного. Можно кэшировать приватный ключ, и это будет безопаснее, но можно будет производить только приватные ключ. А можно кэшировать публичный ключ, и создавать как приватные от приватных, так и публичные от публичных, но это создает больше рисков для безопасности и требует более надежного подхода к хранению публичных ключей.

Главная мысль по гомоморфизму: **мы можем создавать производные публичные ключи, выполняя операции над существующим ключом или ключами.** Чтобы получить приватный ключ от производного публичного, нужно выполнить те же операции над соответствующими приватными ключами.

В формулах это будет выглядеть так:
1. `pch = ppar + hash` — дочерний приватный ключ (`pch`) образуется от прибавления к родительскому ключу (`ppar`) хэша, взятого от публичного ключа `Ppar`.
2. `Pch = Ppar + point(hash)` — дочерний публичный ключ (`Pch`) образуется  от прибавления к родительскому ключу (`Ppar`) точки, взятой от хэша, взятого от `Ppar` (взятие точки — это операция получения публичного ключа от хэша).

И там, и там добавляем один и тот же хэш. В итоге получается, что `Pch` это публичный ключ от приватного ключа `pch`, но мы его получили, не имея приватный.
Такая вот магия. Это всё возможно благодаря эллиптическим кривым. И эта свойство также сделало возможным, например, Тапрут.